apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:

  replicas: 1
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:1.24.1
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80